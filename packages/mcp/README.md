# MCP

MCP integration package.

Responsibilities:

- MCP server adapter for Capability Node
- MCP client adapter for Agent Control Plane
- existing MCP server adapter provider

This package adapts MCP to internal ToolCall contracts.
