# Core

Framework-independent domain contracts and interfaces.

This package must not depend on Kratos, MCP SDKs, databases, or provider
implementations.

It will contain:

- Provider interface
- ToolCall types
- Signal types
- Event types
- Workspace and Environment abstractions
- Policy interfaces
