package telemetry

const (
	MetricResourceTransferBytesTotal = "acorn.resource.transfer.bytes_total"
	MetricResourceTransferTotal      = "acorn.resource.transfer.total"

	MetricWorkspaceExecTotal           = "acorn.workspace.exec.total"
	MetricWorkspaceExecDurationSeconds = "acorn.workspace.exec.duration_seconds"

	MetricWorkspaceLeaseBusyTotal = "acorn.workspace.lease.busy_total"
)
