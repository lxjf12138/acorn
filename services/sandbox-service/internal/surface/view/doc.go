// Package view contains sandbox-service view-surface boundaries.
//
// The view surface provides temporary, read-only, user-facing browse and
// preview operations through the control plane. View operations do not create
// ResourceRefs and do not persist workspace file indexes.
package view
