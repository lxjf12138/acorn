// Package sandbox contains sandbox-service sandbox domain boundaries.
//
// A sandbox is an execution context managed by sandbox-service. It may mount a
// service-owned workspace, but it does not own the workspace lifecycle.
package sandbox
