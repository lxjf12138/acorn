// Package importexport contains sandbox-service ResourceRef import and export
// domain boundaries.
//
// Cross-service files enter a workspace only through explicit import. Workspace
// files leave sandbox-service only through explicit export to ResourceRef.
package importexport
